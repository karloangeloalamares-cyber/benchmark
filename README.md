# Southern University Benchmark MVP

Local Expo React Native foundation for the Southern University Benchmark visual MVP.

## Scripts

- `npm start` starts Expo.
- `npm run web` starts Expo for web preview.
- `npx expo export --platform web` creates a local web export in `dist`.
